# ROADMAP

> **滚动计划**——唯一计划源，可自由增删改。STATUS.md 记录历史，`docs/wiki/` 记录代码事实，本文件只记录未来。
> 完成的条目直接删除（历史在 STATUS 里查）。条目格式：`- **标题** — 为什么做。验收：怎样算完成。入口：直接做 / /wayfinder`。

## Now（进行中 / 下一两个会话就做，≤ 3 条）

-

## Next（排队，按优先级排序）

- **Wails 版本升级对齐** — go.mod v2.12.0 落后 CLI v2.13.0，构建时打印提示。验收：go.mod 升级后 `wails build` 无版本告警、全量测试绿。入口：直接做。

## Later（想法池，防遗忘，写松一点）

- Release 推广（v1.1.1 已发布，后续看渠道）
- 新 Provider 扩展（按用户需求，入口见 docs/ADDING_A_PROVIDER.md）
- 用户反馈迭代
- Kimi 细节打磨 — 倒计时改取"正在驱动告警的窗口"的重置时间（现在固定 5h 窗口）；官方页「总使用量」总额度字段未纳入监控。验收：用户提出即做。入口：直接做

## 已升级 / 已否决（留痕，防止重复提出）

- UI 效果截图记录 → 已否决：用户明确不需要截图（2026-08-25）
